# Basic Usage Sample

1. Import this sample via Package Manager (Samples > Basic Usage > Import).
2. Create a new scene or use an existing one.
3. Create an empty GameObject and add the `HapticsSample` component (uses `MythicStudio.Haptics` namespace).
4. Build for iOS or Android and run on a device.
